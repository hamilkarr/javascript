제어문

1. 조건문
    ㄱ) if (조건식) { //if 조건식이 참 일때 실행되는 영역 }

    ㄴ) 조건식에는 주로 비교, 논리 연산자를 사용한다. 
    
    ㄷ) if (조건식) { //if 조건식이 참일때 실행 } 
                else { //if 조건식이 거짓일때 실행 }

    ㄹ) 조건식이 여러개인 경우 else if               
        if (조건식1) {//if 조건식1이 참일때 실행} else if (조건식2) {// 조건식1이 거짓이고 조건식2가 참일때 실행 영역} else {// 모든 조건식이 거짓일때 실행되는 영역}

    ㅁ) 조선식을 여러개 비교 --> 논리 연산자와 결합
        if (조건식1 || 조건식2) {//조건식 }

    ㅂ) or 조건인 경우 우선순위 유의 : true를 결정하는 우선 순위가 앞쪽부터 체크되므로 >> ()를 씌워서 해결한다.

    ex) ((city == '인천 || city == '서울) && school == '초등학교' && grade == '6학년') {console.log('인천 또는 서울에 사는 초등학교 6학년);}

2. 선택문
    ㄱ) 선택을 하는 구문
        switch(키워드) { case "키워드1" : 처리할 영역 
                        break; <- 실행을 멈춤
                        case "키워드2" : 처리할 영역 
                        default: 매칭이 하나도 안될 경우 }
    ex)

3. 반복문
    반복을 할때 쓰는 구문: 반복을 위한 조건식이 필수.

    ㄱ) while (조건식) { //조건식이 참일때 반복되는 영역}

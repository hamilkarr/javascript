Math
    수학과 관련된 생성자
    .abs() 절대값 ->
        음수 >> 양수
        양수 >> 음수

    .round() > 반올림
    .floor() > 버림
    .ceil() > 올림
   
    .max() > 최대값
    .min() > 최소값
    .pow(x,y) x의 y제곱
    .sign() >> 부호: 음수(-1), 양수(1) 0(0)
    .sqrt() >> root 
        ex)math.sqrt(9) >> 3
    .random() >> 난수(무작위수)를 발생시키는 매서드(0~1사이의 무작위수)